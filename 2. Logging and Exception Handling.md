# Creating Project Structure, Logging, and Exception Handling in an End-to-End ML Project

## 1. Overview of the Session

In the previous session:

* `setup.py` was created to **build a Python package**
* `requirements.txt` was created to **store required dependencies**
* A **virtual environment** was created and activated
* Initial project setup was completed

In this session, the focus is on:

1. Creating the **complete project folder structure**
2. Understanding **modular coding**
3. Implementing **logging**
4. Implementing **custom exception handling**
5. Creating **common utility files**

All these steps are necessary to make the project follow **industry-level clean code practices**.

---

# Part 1: Creating the Project Structure

All the main code of the project will be placed inside the **`src` folder**.

Currently, the `src` folder contains:

```
src/
 └── __init__.py
```

The `__init__.py` file makes the folder a **Python package**, allowing modules to be imported from it.

---

# Part 2: Creating the Components Folder

Inside `src`, create a folder called:

```
components
```

Immediately after creating any folder, create:

```
__init__.py
```

### Why do we create `__init__.py`?

Because:

* It makes the folder a **Python package**
* Allows modules inside the folder to be **imported into other files**

Example:

```
src/
 └── components/
      └── __init__.py
```

---

# Part 3: Understanding Components

Components are **individual modules of the ML pipeline**.

Each component performs a **specific task**.

Example tasks:

* Reading data
* Transforming data
* Training model
* Evaluating model

Each task will have its **own Python file**.

---

# Part 4: Creating the First Component – Data Ingestion

Create a file inside `components`:

```
data_ingestion.py
```

### Purpose

Data Ingestion means:

Reading the dataset from sources like:

* Database
* CSV files
* APIs
* Cloud storage

### Responsibilities of this file

* Read dataset
* Split dataset into:

```
Train Dataset
Test Dataset
Validation Dataset
```

All code related to **loading and splitting data** will be written here.

---

# Part 5: Creating Data Transformation Component

Create another file:

```
data_transformation.py
```

### Purpose

This file will contain code for **data preprocessing**.

Examples of transformations:

* Converting categorical features → numerical features
* One-Hot Encoding
* Label Encoding
* Feature scaling
* Feature engineering

All transformation logic is placed here.

---

# Part 6: Creating Model Trainer Component

Create another file:

```
model_trainer.py
```

### Purpose

This file is responsible for **training machine learning models**.

Typical tasks inside this file:

* Training different ML models
* Evaluating model performance
* Calculating metrics

Examples:

For classification problems:

* Confusion Matrix
* Accuracy
* Precision
* Recall

For regression problems:

* R² Score
* Adjusted R² Score

---

### Model Storage

The trained model will be saved as a **pickle file**.

Later it can also be **pushed to cloud storage**.

---

# Current Components Structure

```
src/
 └── components/
      ├── __init__.py
      ├── data_ingestion.py
      ├── data_transformation.py
      └── model_trainer.py
```

These components mainly handle the **training process**.

---

# Part 7: Creating the Pipeline Folder

Next create another folder inside `src`:

```
pipeline
```

Create:

```
__init__.py
```

---

## Why Pipelines Are Needed

Pipelines **connect all components together**.

They define **the order in which components run**.

Two pipelines will be created:

1. Training Pipeline
2. Prediction Pipeline

---

# Training Pipeline

Create file:

```
train_pipeline.py
```

### Purpose

This file will:

* Call `data_ingestion`
* Call `data_transformation`
* Call `model_trainer`

It basically **orchestrates the entire training workflow**.

---

# Prediction Pipeline

Create file:

```
predict_pipeline.py
```

### Purpose

After the model is trained, this pipeline:

* Takes new input data
* Applies preprocessing
* Loads trained model
* Generates predictions

---

# Pipeline Folder Structure

```
src/
 └── pipeline/
      ├── __init__.py
      ├── train_pipeline.py
      └── predict_pipeline.py
```

---

# Part 8: Creating Common Utility Files

Inside the `src` folder create three important files.

```
logger.py
exception.py
utils.py
```

---

# 1️⃣ logger.py

Purpose:

To **log execution details** of the application.

Example logs:

* Model training started
* Data loaded successfully
* Error occurred

Logs are saved in **log files**.

---

# 2️⃣ exception.py

Purpose:

To implement **custom exception handling**.

Instead of default Python errors, we create **detailed error messages**.

Example information included:

* File name
* Line number
* Error message

---

# 3️⃣ utils.py

Purpose:

Contains **common reusable functions**.

Examples:

* Database connection
* Cloud storage upload
* Model saving
* Data loading

Any code used in multiple places goes here.

---

# Complete Project Structure

```
ml_project/

│
├── src/
│
│   ├── components/
│   │    ├── __init__.py
│   │    ├── data_ingestion.py
│   │    ├── data_transformation.py
│   │    └── model_trainer.py
│
│   ├── pipeline/
│   │    ├── __init__.py
│   │    ├── train_pipeline.py
│   │    └── predict_pipeline.py
│
│   ├── logger.py
│   ├── exception.py
│   └── utils.py
│
├── setup.py
├── requirements.txt
└── README.md
```

---

# Part 9: Implementing Custom Exception Handling

Inside:

```
exception.py
```

Copy this Code:

```python
import sys
from src.logger import logging

def error_message_detail(error,error_detail:sys):
    _,_,exc_tb=error_detail.exc_info()
    file_name=exc_tb.tb_frame.f_code.co_filename
    error_message="Error occured in python script name [{0}] line number [{1}] error message[{2}]".format(
     file_name,exc_tb.tb_lineno,str(error))

    return error_message

    

class CustomException(Exception):
    def __init__(self,error_message,error_detail:sys):
        super().__init__(error_message)
        self.error_message=error_message_detail(error_message,error_detail=error_detail)
    
    def __str__(self):
        return self.error_message
    

```

### Purpose of sys module

The `sys` module provides information about the **Python runtime environment**.

It helps retrieve:

* File name where error occurred
* Line number
* Execution details

---

# Part 10: Implementing Logging

Inside:

```
logger.py
```

Copy this Code:

```python
import logging
import os
from datetime import datetime

LOG_FILE=f"{datetime.now().strftime('%m_%d_%Y_%H_%M_%S')}.log"
logs_path=os.path.join(os.getcwd(),"logs",LOG_FILE)
os.makedirs(logs_path,exist_ok=True)

LOG_FILE_PATH=os.path.join(logs_path,LOG_FILE)

logging.basicConfig(
    filename=LOG_FILE_PATH,
    format="[ %(asctime)s ] %(lineno)d %(name)s - %(levelname)s - %(message)s",
    level=logging.INFO)
```
# Git Commands to Save the Project

Check files:

```
git status
```

Add all files:

```
git add .
```

Commit changes:

```
git commit -m "logging and exception"
```

Push to GitHub:

```
git push -u origin main
```

This updates the project repository on GitHub.

---

# Step-by-Step Summary

### Project Structure Creation

1. Create `src` folder.
2. Add `__init__.py` to make it a Python package.
3. Create `components` folder.
4. Add `__init__.py` inside components.
5. Create component files:

   * `data_ingestion.py`
   * `data_transformation.py`
   * `model_trainer.py`.

---

### Pipeline Creation

6. Create `pipeline` folder.
7. Add `__init__.py`.
8. Create:

   * `train_pipeline.py`
   * `predict_pipeline.py`.

---

### Utility Files

9. Create:

   * `logger.py`
   * `exception.py`
   * `utils.py`.

---

### Exception Handling Implementation

10. Import `sys`.
11. Create `error_message_detail()` function.
12. Extract traceback information.
13. Create formatted error message.
14. Create `CustomException` class.
15. Override `__str__` method.

---

### Logging Implementation

16. Import `logging`, `os`, `datetime`.
17. Create log file name using timestamp.
18. Create logs directory.
19. Configure logging with `logging.basicConfig()`.
20. Define logging format.
21. Test logger with `logging.info()`.

---

### Testing

22. Run logger test file.
23. Verify log file creation.
24. Test exception handling using try-except block.

---

### Version Control

25. Check Git status.
26. Add files.
27. Commit changes.
28. Push code to GitHub repository.

---


